var app = app || {};

app.AppView = Backbone.View.extend({
  el: '#main',
  render: function() {
    console.log('Rendering AppView...');
    // var html = // template html
    // this.$el.html(html);
  }
});
